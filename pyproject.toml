[project]
name = "pacman"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "json5>=0.14.0",
    "mazegen",
    "pydantic>=2.13.3",
]

[tool.uv.sources]
mazegen = { path = "mazegen-0.1.0.tar.gz" }
